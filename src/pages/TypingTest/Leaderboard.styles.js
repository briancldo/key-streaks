import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
  leaderboard: {
    position: 'absolute',
    top: '2rem',
    left: '2rem',
    height: 'auto',
    width: '20%',
    textAlign: 'center',
    overflow: 'auto',
  },
});

export { useStyles };
