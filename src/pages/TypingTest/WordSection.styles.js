import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
  card: {
    height: '20%',
    width: '80%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    paddingLeft: '2.5%',
    paddingRight: '2.5%',
  },
});

export { useStyles };
